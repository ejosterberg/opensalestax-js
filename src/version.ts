// SPDX-License-Identifier: Apache-2.0

/**
 * Package version. Kept in sync with `package.json` manually on each
 * release. Used as the default User-Agent suffix when the client makes
 * requests so engine operators can see which SDK version they're
 * talking to in their access logs.
 */
export const VERSION = '0.1.0';
